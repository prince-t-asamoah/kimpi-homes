import { AuthFormValidators } from './auth-form.validators';

describe('AuthFormValidators', () => {
  it('should create an instance', () => {
    expect(new AuthFormValidators()).toBeTruthy();
  });
});
